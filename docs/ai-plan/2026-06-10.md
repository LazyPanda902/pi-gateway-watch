# AI Daily Plan - 2026-06-10

## Provider

claude

## Status

ok

## Limited

False

## Task

Suggest one small real daily improvement for pi-gateway-watch. Project angle: IT support automation. Prefer a safe change such as docs, tests, config example, parser improvement, or a small zero-dependency feature. Do not suggest fake progress.

## Output

I have enough context. The project is in stub state with a config-example.md that already defines disk/load monitoring thresholds. The best real next step is to implement those thresholds as actual stdlib code.

---

**1. Summary**

Add a `system_health()` function to `src/app.py` that reads disk usage and load average using Python stdlib only (`shutil.disk_usage`, `os.getloadavg`). This directly operationalizes the thresholds already defined in `docs/config-example.md` (`warning_disk_percent`, `critical_disk_percent`) and gives the project a real, runnable IT support feature.

**2. Proposed files to change**

- `src/app.py` — add `system_health(path, warn_pct, crit_pct)` function
- `tests/test_app.py` — add tests for the new function (mocked disk/load values, no real I/O)

**3. Safety notes**

- Zero external dependencies — stdlib only (`os`, `shutil`)
- `os.getloadavg()` is Linux/macOS only; guard with `hasattr` so it degrades gracefully on Windows
- No secrets, no network calls, no subprocess
- Mocked in tests — no real disk paths touched during CI

**4. Commit message**

```
feat(health): add stdlib system_health parser for disk and load monitoring
```

---

Shall I implement this now?

## Safety

This plan is inspiration only. The automation applies controlled changes and must still pass checks before commit.
